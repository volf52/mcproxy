package version

const (
	// Version is the semantic version of mcproxy
	Version = "1.0.0"
	// GitCommit is the git commit hash (set during build)
	GitCommit = "unknown"
	// BuildDate is the build date (set during build)
	BuildDate = "unknown"
)
